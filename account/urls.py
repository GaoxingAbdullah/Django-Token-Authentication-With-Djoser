
from django.urls import path, include

from . import views

urlpatterns = [
    # path('api/auth/users/', include('djoser.urls)')),
    # path('api/auth/activation/', include('djoser.urls.authtoken')),
    # path('api/token/login', include('djoser.urls.jwt')),
    # path('api/token/logout', include('djoser.urls.jwt')),
]
